// import axios from "axios";
//
// const API = axios.create({ baseURL: "http://localhost:5000" });
//
// export const login = (credentials) => API.post("/users/login", credentials);
// export const getTopics = (token) => API.get("/topics", { headers: { Authorization: `Bearer ${token}` } });
// export const markSubtopicCompleted = (subtopicId, token) =>
//     API.patch(`/subtopics/${subtopicId}`, {}, { headers: { Authorization: `Bearer ${token}` } });
